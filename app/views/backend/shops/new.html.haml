%h1= t("New shop")
= form_for [:backend, @shop], html: {class: "form-horizontal", role: "form", id: "branch_form"} do |f|
  = render 'form', {f:f}
= link_to t('Back'), backend_shops_path, :class=>"btn btn-primary"
