<% if @article.errors.size > 0 %>
  $("#article_modal .modal-body").html("<%= escape_javascript(render 'backend/articles/form', {remote: true, article: @article, form_url: backend_shop_material_articles_path(@current_shop.slug, @material) } )%>");
<% else %>
  $("#article_list_show #material_<%= @material.id%> .widget-body .widget-main").html('<%= escape_javascript(render "backend/articles/list", {material: @material} )%>');
  $('#article_modal').modal('hide')
<% end %>
