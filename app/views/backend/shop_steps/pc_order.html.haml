= render layout: 'shop_form' do |f|
  .panel.panel-default
    .panel-heading
      网页下单设置
    .panel-body
      .field.form-group
        = f.label "PC端域名", class: "col-sm-3 control-label"
        .col-sm-6
          通过您的域名让客户访问!
    .panel.panel-footer
      %center
        - if params[:edit]
          = link_to_back
          = f.submit :class=>"btn btn-primary"
        - else
          = link_to '上一步', previous_wizard_path, :class=>"btn btn-primary"
          = f.submit "完成" ,:class=>"btn btn-primary"